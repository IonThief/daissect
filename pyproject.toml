[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "daissect"
version = "0.0.1"
description = "Attached plugins to analyze neural network internal states"
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [
  { name = "Ibrahim Sayed" },
]
dependencies = [
  "torch",
]
